# 2026-09-22：半小时定时操盘＋事件触发

删除AI设定“若干分钟后再看”的到期触发，以及同日刚加入的到期变化门槛。模型只由默认半小时任务或有效事件唤醒。

- 默认半小时轮次检查全部持仓，候选按原有容量及最久未复核优先的规则轮转。旧计划的复核时间不再阻止进入候选范围；不是承诺全部候选每半小时都进入模型。
- 价格穿越模型设定的结构条件、结构风险、持仓急跌、新闻/研究变化、新机会及持仓变化继续触发事件。并非每次行情数值变化都调用模型。
- 三分钟监测和每分钟队列检查继续运行，本身不等于调用模型。无每日事件额度、无额外账户冷却；账户互斥、事件去重、交易时段保护继续保留。
- 删除模型协议的review_after_minutes、next_review_at计算和候选时间过滤。历史计划保留在数据库，读取时去掉旧时间字段；旧模型输出仍可兼容，但时间字段不再生效。
- 遗留pending的review_due、holding_review_due在领取队列时标记expired，不会触发模型。已处理记录保持审计用途，processing或不确定执行继续沿用原有处理，不自动重放。
- 事件候选30只、深度研究候选3只、定时候选8只不变，持仓不占候选额度。普通研究到期在定时轮处理。
- 实盘建议的事实去重、无动作事件静默、账户状态冲突修复继续生效（随后按用户要求撤回问题4新增的T+1风险预算及强制字段，原有T+1提示和可卖量校验保留），成交校验不变。

验证覆盖：候选/持仓在旧复核时间之后、缺少比较基准或价格变化时都不生成到期事件；旧时间不阻塞定时候选且容量轮转有效；旧待处理事件失效且有效新闻事件仍被领取；价格穿越和结构风险继续触发；实际持仓事件不占候选名额。全量unittest结果见运行目录 `.run/event-only-20260922/tests.log`。

无需修改cron；现有任务指向试运行目录，下一轮启动自动加载新代码。验证没有执行交易或发送交易消息。
